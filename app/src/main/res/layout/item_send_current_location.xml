<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="@layout/bottom_sheet_show_locations"
    >

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/navigation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_compass"
        android:layout_margin="@dimen/_medium"
        android:padding="@dimen/_small"
        />

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/navigation"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="15dp"
        android:textSize="@dimen/medium_text"
        android:textColor="@color/black"
        android:layout_marginStart="@dimen/_medium"
        android:text="Send your current location"
        />

    <TextView
        android:id="@+id/subTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/navigation"
        app:layout_constraintTop_toBottomOf="@id/title"
        android:textSize="@dimen/small_text"
        android:textColor="@color/grey"
        android:layout_marginStart="@dimen/_medium"
        android:text="Accurate into 6 meter"
        />

</merge>